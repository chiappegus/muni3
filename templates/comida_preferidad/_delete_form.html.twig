<form method="post" action="{{ path('comida_preferidad_delete', {'id': comida_preferidad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ comida_preferidad.id) }}">
    <button class="btn">Delete</button>
</form>
